NFL Stadium Wallet has been installed.

Services (ClusterIP):
  api-customers : port 8080 (TCP)
  api-bills     : port 8081 (TCP)
  api-raiders   : port 8082 (TCP)
  webapp        : port 5173 (TCP)

Only the webapp has a Route. The webapp Apache proxies /api-customers, /api-bills, /api-raiders to the internal API services, so the browser uses the same origin and you avoid "Unexpected token '<'" (HTML instead of JSON).

{{- if .Values.webapp.route.enabled }}

Webapp Route URL:
  oc get route webapp -n {{ .Release.Namespace }}
{{- else }}

To expose the webapp: --set webapp.route.enabled=true
{{- end }}

To disable the webapp Route: --set webapp.route.enabled=false
